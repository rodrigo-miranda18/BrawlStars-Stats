import {Container} from "../../styles/containerContentPages" 
import BtnRankingPage from "../BtnRankingPage"

const ContentRankingBrawlers = () => (
  <Container>
    <BtnRankingPage isPressed="Brawlers"/>
  </Container>
)

export default ContentRankingBrawlers